import React from 'react';
import { Text, View } from 'react-native';
import Card from './Card';
import RadioBtn from './RadioButtons.js'

let rsp = []

function MockTest() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <RadioBtn/>
      </View>
    );
  }

  
export default MockTest;
